import * as React from "react";
import { Card, CardContent, CardHeader } from '@mui/material';

export default () => (
    <Card>
        <CardHeader title="Welcome to the communi Hr panel" />
        <CardContent>Production mode</CardContent>
    </Card>
);